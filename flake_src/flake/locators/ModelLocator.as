package flake.locators
{
  import flake.ObjectUtils;
  
  import mx.events.PropertyChangeEvent;
  
  public class ModelLocator extends Locator
  {
    
    public function ModelLocator()
    {
      checkEquals = true;
    }
    
  }
}